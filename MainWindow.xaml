<Window x:Class="Gym.Net.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:Gym.Net"
        mc:Ignorable="d"
        Title="SisBurpee - Sistema de Acesso" 
        Height="720" 
        Width="1280"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize">
    <Grid>
        <wv2:WebView2 x:Name="webView"
                  DefaultBackgroundColor="White"/>
        
        <!-- Loading Overlay -->
        <Grid x:Name="loadingOverlay" Background="#F5F5F5">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🏋️ SisBurpee" 
                          FontSize="48" 
                          FontWeight="Bold" 
                          Foreground="#2563EB"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"/>
                <TextBlock Text="Inicializando sistema..." 
                          FontSize="16" 
                          Foreground="#666"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
